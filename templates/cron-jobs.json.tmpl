{
  "jobs": [
    {
      "id": "__MAINT_ID__",
      "name": "Daily memory maintenance",
      "prompt": "You are __AGENT_NAME__ running bounded daily memory maintenance. The attached snapshot is read-only ground truth. Work only inside __AGENT_ROOT__/vault/brain and through your OWN wrapper at __AGENT_ROOT__/.local/bin/gbrain. Finish in at most 8 model calls.\n\nRETRIEVAL BUDGET. Use exactly one session_search discovery with limit=1 for recent durable decisions; never read or scroll a full session. Use at most two focused gbrain retrievals via your own wrapper or MCP. If the snapshot and bounded retrieval expose no new durable fact, stop after the checks and report green.\n\nCONSOLIDATE. Save only durable, high-confidence facts into an existing topical page under vault/brain, updating in place. Reject secrets, PII, transcripts, PR numbers, SHAs, one-off completions, and low-confidence inference. Never edit USER.md or MEMORY.md and never create dated dump pages. Read a target before changing it and make no write when its meaning is unchanged.\n\nHYGIENE. Check only the retrieved evidence for wrong identity, misspelled contact data, inferred birthdays or ages, brittle meeting times, obsolete architecture, and split-brain storage. Correct only facts directly disproved by current evidence; otherwise flag them.\n\nVERIFY. Run __AGENT_ROOT__/.local/bin/gbrain import __AGENT_ROOT__/vault/brain --no-embed, then embed --stale, stats, and one retrieval for any page changed. Require the CLI and MCP to resolve to the same profile-local store. Report green/amber/red with what changed, rejected, and verified. Stay silent unless Chief needs to act.",
      "skill": "memory-architecture",
      "script": "memory_health_snapshot.py",
      "enabled_toolsets": ["file", "terminal", "session_search", "memory"],
      "workdir": "__AGENT_ROOT__",
      "schedule": { "kind": "cron", "expr": "0 23 * * *", "display": "23:00 daily" },
      "enabled": true
    },
    {
      "id": "__SYNC_ID__",
      "name": "Daily identity git-sync",
      "prompt": "Deterministic backup of __AGENT_NAME__'s durable identity to its own private git repo. The script stages (gitignore-scoped to identity only), runs a secret tripwire, commits, and pushes. Runs after memory maintenance so it captures the night's new pages. Stays silent when nothing changed.",
      "script": "git-sync.sh",
      "no_agent": true,
      "schedule": { "kind": "cron", "expr": "0 0 * * *", "display": "00:00 daily" },
      "enabled": true
    }
  ],
  "updated_at": "__NOW__"
}
