# Honcho .env for __PROJECT__. Models default to local Ollama; override as needed.
NAMESPACE=__PROJECT__

# Embeddings (locked dimension — must match across the fleet).
EMBEDDING_VECTOR_DIMENSIONS=768

# Deriver / dialectic / dream model config — set to your providers.
# Examples (local Ollama):
#   DERIVER_MODEL_CONFIG__MODEL=granite4.1:3b-ctx32k
#   DIALECTIC_LEVELS____DLEVEL____MODEL_CONFIG__MODEL=granite4.1:3b-ctx8k
DERIVER_FLUSH_ENABLED=true
